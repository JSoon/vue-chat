# NOTE: 环境变量请根据项目实际修改

# 构建环境
VUE_APP_ENV = 'staging'

# 发布路径
VUE_BASE_URL = '/hl-web/'

# 平台接口地址
VUE_APP_BASE_API = 'http://10.100.20.135:8882'

# AI接口地址
VUE_APP_AI_API = '/'

# 代码生成预览地址
VUE_APP_CODE_PREVIEW_URL = '/'

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = 'gzip'
